#pragma once

/*
 * Shared fallback for RP2xxx board-specific ArduPilot policy overrides.
 *
 * Boards can place a file with this same name in their own hwdef directory
 * to override defaults from libraries/AP_HAL/board/rp2xxxchibios.h.
 */
